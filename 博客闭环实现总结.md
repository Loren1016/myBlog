# 博客功能闭环 — 实现总结

## 1. 修改的文件

### 后端（4 个文件修改，1 个新增）

**修改：**
- [backend/.../entity/Blog.java](backend/src/main/java/com/myblog/entity/Blog.java) — `tags` 字段 `columnDefinition` 从 JSON 改为 VARCHAR(500)，避免 MySQL JSON 列拒绝纯字符串
- [backend/.../repository/BlogRepository.java](backend/src/main/java/com/myblog/repository/BlogRepository.java) — 新增 `findByStatus(String, Pageable)` 分页查询
- [backend/.../service/BlogService.java](backend/src/main/java/com/myblog/service/BlogService.java) — 新增 `findById`、`findPublished`（仅已发布）、`findBySlugPublished`（slug + 已发布校验）、`findAllPaginated`（管理端全量分页）；`create` 方法自动设置 publishedAt
- [backend/.../config/SecurityConfig.java](backend/src/main/java/com/myblog/config/SecurityConfig.java) — 新增 `/api/posts/**` 为 permitAll
- [backend/.../controller/BlogController.java](backend/src/main/java/com/myblog/controller/BlogController.java) — 改为双路径映射 `/api/blogs` + `/api/posts`，仅返回已发布文章，返回分页结构

**新增：**
- [backend/.../controller/AdminBlogController.java](backend/src/main/java/com/myblog/controller/AdminBlogController.java) — 管理员博客 CRUD 端点，路径 `/api/admin/posts`

### 前端（7 个文件修改，5 个新增）

**修改：**
- [frontend/src/lib/api.ts](frontend/src/lib/api.ts) — 新增 `Blog`、`BlogInput`、`PaginatedPosts` 类型；新增 `getPublishedBlogs`、`getPublishedBlogBySlug`、`adminListPosts`、`adminGetPost`、`adminCreatePost`、`adminUpdatePost`、`adminDeletePost`
- [frontend/src/app/admin/page.tsx](frontend/src/app/admin/page.tsx) — 仪表盘卡片链接改为 `/admin/posts` 和 `/admin/posts/new`
- [frontend/src/app/blog/page.tsx](frontend/src/app/blog/page.tsx) — 从静态 mock 数据改为调用 `getPublishedBlogs()` API，变为 async 服务端组件
- [frontend/src/app/blog/[slug]/page.tsx](frontend/src/app/blog/[slug]/page.tsx) — 从静态 mock 数据改为调用 `getPublishedBlogBySlug(slug)` API，使用 `notFound()` 处理 404；通过 `MarkdownContent` 客户端组件渲染 Markdown 正文
- [frontend/src/components/ui/FadeIn.tsx](frontend/src/components/ui/FadeIn.tsx) — TypeScript 兼容修复（`keyof JSX.IntrinsicElements` → `ElementType`）
- [frontend/package.json](frontend/package.json) — 新增 `react-markdown` 和 `remark-gfm` 依赖

**新增：**
- [frontend/src/app/admin/posts/page.tsx](frontend/src/app/admin/posts/page.tsx) — 管理端文章列表页（含 AuthGuard、删除确认、新建按钮）
- [frontend/src/app/admin/posts/PostEditor.tsx](frontend/src/app/admin/posts/PostEditor.tsx) — 共享文章编辑器组件（新建/编辑复用），包含标题、slug、摘要、正文(Markdown textarea)、分类、标签、封面图、状态管理、保存草稿/发布
- [frontend/src/app/admin/posts/new/page.tsx](frontend/src/app/admin/posts/new/page.tsx) — 新建文章页面壳
- [frontend/src/app/admin/posts/[id]/page.tsx](frontend/src/app/admin/posts/[id]/page.tsx) — 编辑文章页面壳
- [frontend/src/app/blog/[slug]/MarkdownContent.tsx](frontend/src/app/blog/[slug]/MarkdownContent.tsx) — 客户端 Markdown 渲染组件，样式匹配 Polene 设计系统

## 2. 新增接口和页面

### API 接口

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/api/posts` | 无 | 公开 - 已发布文章列表（分页） |
| GET | `/api/posts/{slug}` | 无 | 公开 - 单篇文章详情（草稿返回 404） |
| GET | `/api/blogs` | 无 | 公开 - 同上（向后兼容） |
| GET | `/api/blogs/{slug}` | 无 | 公开 - 同上（向后兼容） |
| GET | `/api/admin/posts` | Bearer Token | 管理 - 全部文章列表 |
| GET | `/api/admin/posts/{id}` | Bearer Token | 管理 - 按 ID 获取文章 |
| POST | `/api/admin/posts` | Bearer Token | 管理 - 创建文章 |
| PUT | `/api/admin/posts/{id}` | Bearer Token | 管理 - 更新文章 |
| DELETE | `/api/admin/posts/{id}` | Bearer Token | 管理 - 删除文章 |

### 前端页面

| URL | 类型 | 说明 |
|-----|------|------|
| `/blog` | Public | 已发布文章列表（从 API 加载） |
| `/blog/[slug]` | Public | 文章详情（Markdown 渲染） |
| `/admin` | Admin | 仪表盘（含文章管理入口） |
| `/admin/posts` | Admin | 文章管理列表 |
| `/admin/posts/new` | Admin | 新建文章 |
| `/admin/posts/[id]` | Admin | 编辑文章 |

## 3. 博客 CRUD 流程

```
┌──────────────────────────────────────────────────────┐
│ 管理端                                                │
│                                                      │
│  /admin → 登录 (admin@myblog.com / admin123)         │
│       ↓                                             │
│  JWT access + refresh token 存入 localStorage       │
│       ↓                                             │
│  /admin/posts (GET /api/admin/posts)                 │
│       │ 列表页显示全部文章（draft + published）       │
│       ├── 新建 → /admin/posts/new                    │
│       │    POST /api/admin/posts                     │
│       │    → 成功后跳转编辑页                        │
│       ├── 编辑 → /admin/posts/[id]                   │
│       │    GET /api/admin/posts/{id}                 │
│       │    PUT /api/admin/posts/{id}                 │
│       │    两个提交按钮：保存草稿 / 发布              │
│       └── 删除 → 二次确认                            │
│            DELETE /api/admin/posts/{id}               │
│                                                      │
│  AuthGuard 组件包裹所有管理端页面                      │
│  → 自动验证 Token，过期自动刷新，无效则显示登录表单    │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│ 公开端                                                │
│                                                      │
│  /blog (GET /api/posts)                              │
│  → 服务端组件，async fetch，force-dynamic            │
│  → 仅返回 status = "published" 的文章                │
│       ↓                                             │
│  /blog/[slug] (GET /api/posts/{slug})                │
│  → 服务端组件，slug 不存在或草稿 → notFound()        │
│  → MarkdownContent 客户端组件渲染正文                 │
│  → 复用 ImageOrPlaceholder、FadeInStagger 等 UI 组件 │
└──────────────────────────────────────────────────────┘
```

### 数据规则

- `status = "published"` → 公开可见，publishedAt 自动设置
- `status = "draft"` → 仅管理端可见，public API 返回 404
- `publishedAt` 在首次从 draft 变为 published 时自动填入当前时间
- `slug` 唯一约束，创建时需确保不重复

## 4. 如何手动验证

### 前置条件
1. MySQL 8.0 运行中（`127.0.0.1:3306`，数据库 `myblog`）
2. 启动后端：`cd backend && mvn spring-boot:run`
3. 启动前端：`cd frontend && npm run dev`

### 步骤

**A. 公开端验证**
1. 浏览器打开 `http://localhost:3000/blog`
2. 预期看到已发布文章列表（初始为空）
3. 确保页面风格与整站一致

**B. 管理端验证（完整 CRUD）**
1. 浏览器打开 `http://localhost:3000/admin`
2. 使用 `admin@myblog.com` / `admin123` 登录
3. 进入"文章管理" → 点击"新建文章"
4. 填写标题、slug、摘要、正文（Markdown）、分类、标签
5. 点击"保存草稿"（status=draft）或"发布"（status=published）
6. 返回文章列表，确认新文章出现
7. 点击"编辑" → 修改内容 → 保存
8. 如果文章是 published，回到 `/blog` 确认文章已出现在公开列表
9. 如果文章是 draft，确认 `/blog/草稿slug` 返回 404
10. 在管理列表点击"删除" → "确认"，确认文章被删除

**C. API 直接验证**
```bash
# 登录
curl -X POST http://localhost:8080/api/auth/admin/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@myblog.com","password":"admin123"}'

# 用返回的 accessToken 创建文章
curl -X POST http://localhost:8080/api/admin/posts \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"title":"标题","slug":"hello","content":"内容","status":"published"}'

# 公开查询
curl http://localhost:8080/api/posts
curl http://localhost:8080/api/posts/hello
```

### 验证清单
- [ ] 管理员可新建/编辑/删除文章
- [ ] 已发布文章出现在 `/blog` 列表中
- [ ] 草稿不出现在 `/blog` 列表
- [ ] 直接访问草稿 slug 返回 404
- [ ] 未登录访问 `/api/admin/posts` 返回 401
- [ ] Markdown 正文正确渲染（标题、加粗、列表、引用、代码块）
- [ ] 页面风格与网站 Polene 设计系统一致

## 5. 刻意未做的扩展项

以下功能**不在**本次实现范围内，属于后续阶段：

1. **图片上传** — 封面图仅支持输入 URL，不支持从本地上传。`/api/admin/media/upload` 端点已存（之前的骨架）但博客编辑器未对接
2. **Markdown 所见即所得编辑器** — 正文编辑使用纯 textarea，无实时预览、分屏、工具栏。前端使用 `react-markdown` 仅在公开详情页渲染
3. **自动保存草稿** — 编辑器无自动保存，需手动点击"保存草稿"
4. **标签筛选/搜索/分页** — 公开博客列表页的分页/Tag筛选/搜索为静态展示，未对接真实后端过滤
5. **图片上传至编辑器** — 编辑器内不支持粘贴/拖拽图片
6. **文章阅读量统计** — Entity 有 `viewCount` 字段，但访问详情页不会 +1
7. **上一篇/下一篇导航** — 文章详情页底部的导航链接为静态，未实现真实前后篇
8. **批量操作** — 管理列表无批量删除/批量修改标签
9. **文章版本/修订历史** — 无版本管理
10. **作品管理** — `/api/admin/projects` 等管理端点骨架已存但前端未实现
11. **评论系统** — 无
12. **RSS/SEO meta** — 未实现
13. **暗色模式** — 未实现
14. **TOC（文章目录）** — 未实现

以上均为明确的功能边界，遵循"仅实现博客闭环"的要求。
